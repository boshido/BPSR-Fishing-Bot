# This file will be populated as we add bot types
# For now, we'll import available bots dynamically
def get_available_bots():
    """Get list of available bot types"""
    return ["fishing"]  # Will be extended as we add more bots
